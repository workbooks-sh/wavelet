The smallest path worked well: the deterministic screenplay, velocity, and storyboard stages give the pipeline real structure while dry-run generation keeps spend at zero. What surprised me was that the commercial workflow still expects edit-stage artifacts like `cuts.edl` and `captions.json` even for a one-shot, no-VO sanity test. Next time I would add a tiny fixture mode to the workflow so a single-shot dry run can mark asset and edit stages complete without placeholder files.
