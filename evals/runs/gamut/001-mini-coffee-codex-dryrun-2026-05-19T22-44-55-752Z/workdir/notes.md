The smallest path worked: the brief parsed cleanly, the screenplay/velocity/storyboard gates all completed, and the dry-run media calls exercised the paid boundaries with estimated spend well under the $0.50 ceiling. The surprise was render cost in local time rather than API spend: a 1280x720 composition with richer CSS/video background stalled long enough that I simplified the manifest to a 640x360, 15 fps sanity render and kept the generated shot as the asset-stage artifact. Next time I would add a tiny renderer smoke preset for evals so the pipeline can keep the authored look while rendering quickly and deterministically.
