The dry-run path worked well for exercising the end-to-end Gamut shape: the 9-line brief validated, Fountain parsing produced structured screenplay data, velocity and storyboard stages completed, paid asset calls emitted request specs without live spend, and the workflow runner recognized every commercial stage as complete. The surprise was render cost in local debug mode: richer CSS at 720p was too slow for a sanity eval, so I simplified the HTML and rendered a 320x180, 10 fps version that still plays as a single 5-second coffee-product shot. With more room, I would keep the same pipeline but use a prebuilt release renderer or a lower-cost visual style guide that preserves the A24 observational feel without stressing the debug renderer.
